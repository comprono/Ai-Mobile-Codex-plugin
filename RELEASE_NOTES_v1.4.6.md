# AI Mobile 1.4.6

AI Mobile 1.4.6 repairs the stopped-supervisor and false-progress path exposed by the live Job Vibhu task.

- One public `run-program-campaign` call can admit exactly one bounded read-only recovery epoch; replay and mutating recovery remain denied.
- Program caps use actual terminal consumption for future capacity while preserving gross authorization for audit.
- Plan-derived research workers integrate as revision-fenced execution results instead of being mistaken for bootstrap context.
- Read-only workers cannot call write/edit/patch tools and must return their typed JSON directly.
- Mechanical dependency links are normalized without another model call, while invented discovery files and typed-output file writes fail plan assurance before dispatch.
- The exact fresh Job Vibhu clone passed recovery, database-backed context, strong strategy, full-plan budgeting, correct REQ-003/REQ-004 ownership, and one real REQ-003 execution receipt without changing production state or project files.