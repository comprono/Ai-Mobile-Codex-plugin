# AI Mobile 1.4.5

AI Mobile 1.4.5 fixes the installed-resume failure exposed by the live Job Vibhu campaign.

- Failed Director rounds settle once and advance to their persisted reconciliation package.
- Semantically unchanged capacity observations no longer manufacture repeated budget refreshes.
- Unknown provider quota remains unknown, but one exclusive, fixed-cap attempt can proceed with immutable accounting and no automatic retry.
- Exact Director-preflighted read-only command work can use Antigravity only inside its isolated whole-snapshot mutation guard; generic command lanes remain denied.
- Campaign launch can wait briefly for a meaningful durable boundary so the visible console reports more than startup activity.
- The release canary now starts from the exact current Job Vibhu post-failure state and permits only bounded, materially changed foundation recovery.