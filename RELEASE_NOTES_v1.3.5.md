# AI Mobile 1.3.5 - owned recovery and visible unattended progress

AI Mobile 1.3.5 repairs the failure observed in the live Job Vibhu orchestration run.

- Blocked recovery work retains its exact acceptance-linked work-graph node.
- Automatic dispatch refuses orphan work instead of spending a model call that cannot be integrated.
- Rejected orphan observations are persisted once and do not become invisible retry churn.
- A capped deterministic filename scan supplies likely source, fixture, runner, and focused-test paths while excluding stale provider worktrees and transient data.
- Explicit unattended requests can use one finite five-hour coordinator with normal no-progress and resource guards.
- Explicit periodic-report requests can use one passive task-bound heartbeat that reads material status once per interval.

The visible Codex task remains a zero-file project console. Project work stays in separately selected workers, and completion still requires project-specific acceptance evidence.
