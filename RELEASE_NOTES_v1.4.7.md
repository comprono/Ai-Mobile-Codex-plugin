# AI Mobile 1.4.7

AI Mobile 1.4.7 completes the preserved Job Vibhu execution route without repeating context or strategy.

- The guarded plan repair runs the exact confirmed-write command and verifies zero remaining integrity issues.
- Strict receipt normalization preserves a valid completed operation instead of launching another worker.
- Authorized isolated Antigravity patch workers receive their exact write, command, and test permissions.
- Missing `pytest` executables fall back safely to `python -m pytest`.
- Runtime-repaired failures no longer poison provider cooldown or force duplicate work.
- The preserved clone integrates both the operational and code packages; startup preflight is clean and the option-control suite passes 9/9.
- All 39 regression suites, three end-to-end suites, and the MCP self-test pass.
